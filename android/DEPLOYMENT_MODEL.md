# Deployment Model

## Browser
Deploy the existing web/PWA application as the AWES Administrator portal.

## Android
Build this `android/` project as an APK/AAB for technicians.

## Backend
Use one authentication and authorization layer and one shared database.

## Important
Do not treat a browser URL restriction as sufficient security. Role checks must be enforced server-side.
